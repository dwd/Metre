globals:
  default-domain: cassidy.cridland.im
  log:
    level: trace
  healthcheck:
    checks:
      foo.example.org: dave.cridland.net

remote:
  any:
    transport:
          type: s2s
          prefer: direct
          sec: false

local:
  cassidy.cridland.im:
    transport:
      type: internal

